import 'package:flutter/material.dart';

class GemaIcons {
  static const wheelsAlignment = Icon(Icons.time_to_leave);
  static const tiresPressure = Icon(Icons.tire_repair);
  static const register = Icon(Icons.info);
  static const archives = Icon(Icons.archive);
}